# gemini-polyserve

This is a `gemini` plugin that starts `polyserve` before running tests.

## Installation
`npm install gemini-polyserve`

## Configuration

```yml
system:
    plugins:
      polyserve:
        port: 1967
```
